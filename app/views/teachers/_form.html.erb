<div id="teacher_form" class="col-md-6">
    <div class="link_to_new_teacher btn btn-light">
        <h3>S'inscrire en tant que professeur</h3>
    </div>
    <div class="teacher_form">
        <%= form_with(model: teacher, local: true,  class: "register-form", id: "register-form") do |f| %>
        <% if teacher.errors.any? %>
        <div id="error_explanation">
            <h2>
                <%= pluralize(teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>
            <ul>
                <% teacher.errors.full_messages.each do |message| %>
                <li>
                    <%= message %>
                </li>
                <% end %>
            </ul>
        </div>
        <% end %>
        <div class="names row">
            <div class="firstname col-md-6">
                <%= f.label :last_name, "Nom" %>
                <%= f.text_field :last_name, placeHolder: Faker::Name.last_name, autocomplet: 'last_name', required: ""%>
            </div>
            <div class="lastname col-md-6">
                <%= f.label :first_name, "Prénom" %>
                <%= f.text_field :first_name, placeHolder: Faker::Name.first_name, autocomplet: "first_name", required: ""%>
            </div>
        </div>
        <div class="row">
            <div class="birthdate col-md-6">
                <%= f.label :date_of_birth,  "Date de naissance" %>
                <%= f.date_field :date_of_birth, placeHolder: "Date-de-naissance", autocomplet: 'date_of_birth' %>
            </div>
            <div class="address col-md-6" style="">
                <%= f.label :address, "Address" %>
                <%= f.text_field :address, placeHolder: Faker::Address.street_address, autocomplet: 'address', required: ""%>
            </div>
        </div>
        <div class="location row">
            <div class="city col-md-6">
                <%= f.label :city,  "Ville" %>
                <div class="form-select">
                    <select name="city" id="state_teacher">
                        <option value="tana">Antananarivo</option>
                        <option value="tamatave">Tamatave</option>
                        <option value="Majunga">Majunga</option>
                        <option value="Fianarantsoa">Fianarantsoa</option>
                        <option value="Fianarantsoa">Toliara</option>
                        <option value="Fianarantsoa">Antiranana</option>
                        <option value="tana">Autre</option>
                    </select>
                    <span class="select-icon"><i class="zmdi zmdi-chevron-down"></i></span>
                </div>
            </div>
            <div class="est col-md-6">
                <%= f.label :educational_establishment, "Etablissement scolaire" %>
                <select name="educational_establishment" id="educational_establishment">
                    <% EducationalEstablishment.all.each do |est| %>
                    <option>
                        <%= est.title %>
                    </option>
                    <% end %>
                    <option>Autre</option>
                </select>
            </div>
        </div>
        <div class="country">
            <%= f.label :country, "Pays" %>
            <select name="country" id="country">
                <% CS.countries.each do |key, country| %>
                <option>
                    <%= country %>
                </option>
                <% end %>
            </select>
        </div>
        <div class="row">
            <div class="subject col-md-6">
                <%= f.label :subject, "Matière enseignée" %>
                <select name="subject" id="subject">
                    <% Subject.all.each do |subject| %>
                    <option>
                        <%= subject.title %>
                    </option>
                    <% end %>
                </select>
            </div>
            <div class="level col-md-6">
                <%= f.label :degrees, "Diplomes" %>
                <select name="degrees" id="degrees">
                    <% Degree.all.each do |degree| %>
                    <option>
                        <%= degree.title %>
                    </option>
                    <% end %>
                </select>
            </div>
        </div>
        <div class="submit">
            <%= f.submit "S'inscrire", class: "submit" %>
        </div>
        <% end %>
    </div>
</div>