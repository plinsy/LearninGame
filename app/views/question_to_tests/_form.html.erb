<%= form_with(model: question_to_test, local: true) do |form| %>
  <% if question_to_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_to_test.errors.count, "error") %> prohibited this question_to_test from being saved:</h2>

      <ul>
      <% question_to_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
